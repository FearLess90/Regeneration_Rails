<%= simple_form_for(@make, :html => {:class => 'form-horizontal' }) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.button :submit, :class => 'btn-success' %>
<% end %>