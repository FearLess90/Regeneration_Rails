$("#MakesTable tbody").html("<%= escape_javascript(render @makes) %>");
$('#paginator').html('<%= escape_javascript(paginate(@makes, :remote => true, :params => {column:session[:column], direction:session[:direction]}).to_s) %>');