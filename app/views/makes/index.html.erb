<div class="page-header">
  <h2><%=t '.title', :default => Make.model_name.human.pluralize %></h2>
</div>

<table id="MakesTable" class="table table-striped">
  <thead>
    <tr>
      <th class="header"><%= Make.human_attribute_name(:id) %></th>
      <th class="header"><%= Make.human_attribute_name(:name) %></th>
      <th class="header"><%= Make.human_attribute_name(:description) %></th>
      <th class="header"><%= Make.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody id="makes">
  	<%= render @makes %>  
  </tbody>
</table>
<div id="paginator">
	<%= paginate @makes, :remote => true %>
</div>
<script>

</script>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_make_path,
            :class => 'btn btn-primary' %>

