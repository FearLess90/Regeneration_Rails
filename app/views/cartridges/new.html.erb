<%= simple_form_for(@cartridge, :html => {:class => 'form-horizontal' }) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :cartridge_type %>
    <%= f.association :make %>
    <%= f.button :submit, :class => 'btn-success' %>
<% end %>